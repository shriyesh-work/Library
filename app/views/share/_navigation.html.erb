<nav class="navbar navbar-default">
  <div class="container">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#libraryMenu">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Library</a>
    </div>
    <div class="collapse navbar-collapse" id="libraryMenu">
      <% if cookies.signed[:user_id] and @user_in %>
        <% if @user_in.is_admin %>
        <ul class="nav navbar-nav">
          <li class="<%= 'active' if current_page?(dashboard_path) %>"><a href="/dashboard">Dashboard</a></li>
        </ul>
        <% end %>
        <ul class="nav navbar-nav navbar-right">
          <li>
            <a href="login" class="dropdown-toggle" data-toggle="dropdown" data-target="#userMenu"><%= @user_in.firstname+" "+@user_in.lastname %> <span class="caret"></span></a>
            <div class="dropdown" id="userMenu">
              <ul class="dropdown-menu">
                <li><a href="/users/<%= @user_in.id %>"><span class="glyphicon glyphicon-user"></span> Profile</a></li>
                <li><a href="/logout"><span class="glyphicon glyphicon-log-out"></span> Logout</a></li>
              </ul>
            </div>
          </li>
        </ul>
      <% else %>
        <ul class="nav navbar-nav navbar-right">
          <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
          <li><a href="login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>